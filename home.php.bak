<?php get_header(); ?>

<main class="container mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6"><?php single_post_title(); ?></h1>

  <?php if ( have_posts() ) : ?>
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      <?php while ( have_posts() ) : the_post(); ?>
        <article class="bg-white border rounded-lg shadow-sm overflow-hidden">
          <a href="<?php the_permalink(); ?>">
            <?php if ( has_post_thumbnail() ) : ?>
              <img src="<?php the_post_thumbnail_url('medium'); ?>" alt="<?php the_title(); ?>" class="w-full h-48 object-cover">
            <?php endif; ?>
          </a>
          <div class="p-4">
            <h2 class="text-lg font-semibold mb-2">
              <a href="<?php the_permalink(); ?>" class="hover:text-primary"><?php the_title(); ?></a>
            </h2>
            <p class="text-gray-600 text-sm mb-2"><?php the_excerpt(); ?></p>
            <a href="<?php the_permalink(); ?>" class="text-primary hover:underline text-sm">Read more →</a>
          </div>
        </article>
      <?php endwhile; ?>
    </div>

    <div class="mt-8">
      <?php the_posts_pagination(); ?>
    </div>
  <?php else : ?>
    <p>No blog posts yet.</p>
  <?php endif; ?>
</main>

<?php get_footer(); ?>
